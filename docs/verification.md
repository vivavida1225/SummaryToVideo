# 검증 기록

## 2026-09-11 — 등락 표현·보정 요청·분량 검증 수정

- Python 전체 111개 테스트 통과. 급락/폭락/내림세, 급등/폭등/반등/오름세를 인식하며 원문과 반대 방향은 계속 거부함.
- 490~550자 양끝 포함을 필수 범위로 적용. 489/490/500/550/551/650자 경계와 자료 부족 시 미달 거부를 확인함. 기존 400~550자 경고·650자 상한은 새 생성 결과에 사용하지 않음.
- 부족·초과 분량과 현재 줄별 글자 수를 보정 요청에 전달. 원문(user) → 이전 응답(model) → 보정 지시(user) 대화를 구성하고 전체 시스템 지시를 유지함. 보정 지시는 실제 개행 4개·장면 역할·문장 수·분량 보정을 함께 안내함.
- 독립 검토에서 주요 결함 없음. 구조·지수·분량 검증과 SDK 요청 구성 관련 80개 테스트 통과. 총 3회 호출/보정 1회 정책은 유지함.
- 실제 0910 입력: `outputs/20260911_144047_292321_225f3fcc/`. 첫 응답 594자·5줄은 상한 초과로 거부하고, 보정 응답 **496자·5줄**은 통과함. 최종 저장과 실제 Windows 클립보드 내용 일치 확인.
- 실제 최근 급락 입력: `outputs/20260911_143836_607030_8a51f59d/`에서 319자 → 454자로 보정됐으나 하한 미달로 종료. 보정 지시 보강 후 `outputs/20260911_143954_631199_b056d9d5/`에서는 416자 → 452자로 여전히 미달. 두 실행 모두 급락 방향을 인식했고 응답은 5줄을 유지했으며, 실패 응답이 최종 결과로 저장·복사되지 않음.
- 한계: 모델의 첫 생성/보정이 항상 범위를 충족하는 것은 아님. 테스트는 한 입력의 정상 보정·저장과 다른 입력의 정상 실패 차단을 확인한 것이며, 모든 입력의 생성 성공을 보증하지 않음. 인포엑스 음성 재생 시간은 이번 테스트에서 측정하지 않음.

아래 기록의 권장 분량과 테스트 수는 각 당시 버전의 기준이다.

## 2026-09-11 — 5줄 낭독 대본 개정

- Python 전체 95개 테스트 통과. 기존 의존성의 사용 중단 예정 경고 3건은 유지됨.
- Vue 컴포넌트 테스트 12개 통과. TypeScript 검사와 Vite 프로덕션 빌드 통과.
- 5줄 형식, 인사말 제외 문장 수, 소수점, 개행 정규화, 650자 경계, 지수 교환·누락·방향 오류, 보합·쉼표·퍼센트 표현을 검사함.
- 실제 응답에서 발견한 문장 구분용 쉼표, 퍼센트 표기, 부가 등락폭, 장중 반전 서술의 오인식을 회귀 테스트로 추가함. 독립 검토에서 발견한 장중 수치/종가 구분, 인사말 뒤 장면 표식, 반복된 시장 이름도 수정·검증함.
- 저장·재조회·재복사 및 기존 형식 결과의 무변환 조회 테스트 통과. Edge 데스크톱/모바일에서 결과 복원·인증된 다운로드·복사·잘못된 입력 처리 통과. 저장 대본과 실제 Windows 클립보드가 개행 정규화 후 일치함.
- 실제 Gemini 최종 표본: `outputs/20260911_135153_351794_a3a37a55/compressed.txt`. 첫 호출에 통과, 정확히 5줄, 개행 제외 378자. 400~550자 권장 미달 경고가 정상 표시됨.
- 위 표본에서 시장 결과·장중 경로, 유가, 반도체·ETF 자금, 외국인·기관 수급과 리스크, 시장 정의·다음 체크포인트를 확인함. 유가 부담·반도체 자금 집중·차별화 장세라는 세 가지 메시지가 연결됨. 입력의 중복·잠정/확정 뉴스는 별도로 나열되지 않음.
- 해당 표본을 Windows Microsoft Heami Desktop 한국어 음성, Rate=0으로 WAV 합성해 **61.53초**를 측정함. `.runtime/narration-evaluation/full-heami-rate0.wav`에 보관. 영상에 쓰는 음성·속도가 바뀌면 길이도 달라지므로 650자 제한 자체가 60초를 보증하지는 않음.
- 외부 변수·장중 경로·리스크가 없는 혼합 수급 입력은 `tests/fixtures/mixed_flows_missing_context.txt`로 실제 평가함. 자료 부재를 명시하고 외국인 순매수·기관 순매도를 구분함. 초기 응답의 체크포인트 생략을 확인해 계약을 보강했고, 최종 응답은 ETF 자금 지속 여부를 확인 대상으로 포함함. 결과는 `.runtime/narration-evaluation/mixed-flows-missing-context-refined.txt`에 보관(330자, 첫 호출 통과).
- 프롬프트 의미 준수는 표본 검토 결과이며, 자동 검증이 모든 문장의 사실성·인과관계·장면 역할을 보증하지는 않음. 권장 분량 미달은 실패가 아닌 편집 안내임.

아래는 이전 장면 데이터 출력 형식의 초기 구현 검증 기록이다.

## Python / Vue

- Python: 56 tests passed (`.venv/Scripts/python.exe -m pytest tests -q`).
- Vue: 11 component tests passed (`npm --prefix frontend test`).
- TypeScript 검사와 Vite 프로덕션 빌드 통과.
- 별도 검토에서 발견한 HTML wrapper/블록 공백, 결과 폴더 격리, 저장 실패 시 본문 보존, API 오류 형식, 화면 재연결 문제를 수정하고 회귀 테스트를 추가함.
- Python 3.14 환경에서 의존성 내부의 사용 중단 예정 경고 3건이 있으며 테스트 실패는 없음.

## 실제 연결 / 브라우저

- `gemini-3.5-flash-lite` 실제 호출 성공. 첫 응답의 1번 장면 설명 48자 초과를 감지하고, 총 2회 호출 안에서 보정 완료.
- 0910 입력 6개 장면 → 검증된 최종 5개 장면, 본문 333자. 400~550자 권장 범위 미달은 화면 경고로 표시되며 650자 상한과 장면별 제한은 충족.
- 결과: `outputs/20260911_093218_597402_2767fafc/`.
- 최종 파일과 Windows 클립보드 본문 일치 확인 (LF/CRLF 개행만 정규화하여 비교).
- 다른 프로세스가 유효한 창 핸들로 클립보드를 잠근 상태에서 약 0.67초 안에 재시도 종료 및 오류 반환 확인.
- Windows 네이티브 신뢰 저장소로 TLS 연결 검증. 인증서·호스트 이름 검증을 유지함.
- 실제 Edge 브라우저: 파일 미선택 기본값, 잘못된 HTML의 오류 표시, 저장된 작업 복원, 토큰을 포함한 다운로드, 클립보드 재복사 통과.
- 1440px 데스크톱 및 390px 모바일 폭 확인, 가로 넘침·JavaScript 실행 오류 없음.
- 브라우저 결과 화면 캡처는 `.runtime/ui-results.png`, `.runtime/ui-mobile.png`에 보관.

## Windows 실행기

- 실제 Windows 수명주기 검사 통과: 서버 시작, 동일 인스턴스 재사용, 인증된 정상 종료, 8765 점유 시 8766으로 자동 전환 (27.6초).
- 설치 도구의 stderr 경고를 실패로 오인하지 않도록 종료 코드로 판단하고, 자식 프로세스에 프로젝트 Node 경로를 전달함.
- 새 설치의 `.tools` 생성 및 UTF-8 가상 환경 경로 읽기 수정 후 PowerShell 구문 검사 통과.
- 한글·공백이 포함된 복사 경로에서 `start.cmd`, 이동한 가상 환경 복구, 기존 서버 재사용 및 `stop.cmd` 통과.
- 변경 없는 재실행에서 의존성·빌드 확인 파일과 `dist/index.html`의 내용·수정 시각이 유지됨.
- 최종 독립 검토 통과, 미해결 사항 없음. 실행기의 런타임 다운로드 분기는 이미 검증된 런타임이 있어 재실행하지 않음.
- 상세 결과는 `docs/launcher-report.md`, 전체 검토는 `docs/final-review.md`에 기록한다.
